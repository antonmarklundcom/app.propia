# Phase S5 — Store readiness and docs. Paste into a fresh SONNET session, ONLY after S4 is merged. LAST PHASE.

Read `fable/REVIEW.md` and `fable/plan.md` FIRST, in full — plus §9 and `KNOWN-ISSUES.md`.
Execute plan §6.2 under the autonomy protocol §4. Build nothing outside the plan.

HARD LIMITS (§4.7): no changes to types, contract, client transport, `app.json`
identifiers, or propia.node. Adding `extra.eas.projectId` and asset paths to `app.json` is
allowed; renaming ids is not.

Phase rules:
- Branch `phase/s5` off latest `main`. S4 unmerged ⇒ finish it first.
- Load skill `nextjs-deploy-hostinger` only for the README's API/env section wording.
- Human inputs H1–H3 (plan §7) may be absent: then stop at "ready to build" and write the
  exact numbered manual steps; never invent account ids or keys.
- Icons/splash: brand colours from `src/config/brand.ts`, generated by a committed script or
  hand-made PNGs at Expo's required sizes. No paid image generation.
- Docs stay in Swedish; code comments English; UI Spanish. Delete every stale claim listed
  in REVIEW F17. `fable/plan.md` §9 gets the closing entry; §10 Backlog stays.
- Re-runnable; minor issues → `KNOWN-ISSUES.md`; stop only per §4.4.

Exit (all must hold):
- CI green; `npx expo prebuild --no-install` clean with real assets.
- `eas.json` has development/preview/production; production build fails fast if
  `EXPO_PUBLIC_USE_MOCK` is not `false` (guard script proven by running it both ways).
- `grep -ri propia README.md ARCHITECTURE.md API_CONTRACT.md` returns nothing.
- README's dev loop works on a fresh clone exactly as written.
- If H1–H3 present: preview builds submitted to TestFlight and Play internal testing.
- PR merged; §9 closing entry committed.

## After this phase — STOP. Do not spawn any session.
Post the closing report in this window: live API base; store status (submitted, or the
numbered manual steps with which human input each needs); the full exit checklist with
pass/fail; `KNOWN-ISSUES.md` summary; plan §10 Backlog as the suggested next plan. Then
recommend Anton create a project skill (like `propia-dev`) for this app: brand config, data
seam, contract v1, deep-link rule, do-not-touch list.
